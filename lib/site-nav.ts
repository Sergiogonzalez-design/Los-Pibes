/** Shared fixed header + nav bar (compact, matches player profile sites). */
export const siteHeaderClass =
  "fixed top-0 left-0 right-0 z-50 border-b border-white/10 bg-background/85 backdrop-blur-md";

export const siteNavClass =
  "mx-auto flex max-w-7xl items-center justify-between gap-3 px-4 py-2 sm:px-6 lg:px-8";
